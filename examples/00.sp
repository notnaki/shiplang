


struct Hey {
    message: []number;
}

fn hello(x: number) {
    let f = x+10;
    return f;
    
}

impl Hey fn sayHey(self: Hey) {
    self.message[0];
}

let hey = Hey{message:[]number{1,2}};

hey.sayHey(hey);
let a = []number{1,2,3};
a.length();
a = a.pop();
a[0] = 10;
a = a.pop();
a.length();

const l = 10;




let gh = hello(20);

let x = 0;

if (x >= 10) {
    return 10;
} else if (x <= 10) {
    return 20;
} else {
    return 40;
}

while (x < 100) {
    show(x);
    x += 1;
}

foreach (i in range(0,100)){
    show(i);
}

show(range(0,100));

show("end");

show(hey);

struct HelloWorld{
    msg: string;
    num: number;
}

let hellow = HelloWorld{msg:"Hello World", num:42};
show(hellow);
ask("How are you");